import { BuildPurposePipe } from './build-purpose.pipe';

describe('BuildPurposePipe', () => {
  it('create an instance', () => {
    const pipe = new BuildPurposePipe();
    expect(pipe).toBeTruthy();
  });
});
